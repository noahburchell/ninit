#!/bin/bash
#% depon: mounts

swapon -a || true

if [ -f /swap/swapfile ] && ! grep -q '^/swap/swapfile ' /proc/swaps; then
	swapon /swap/swapfile || echo "swap: could not enable /swap/swapfile" >&2
fi
exit 0
