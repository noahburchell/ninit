#!/bin/bash
#%depon: fs
#%onfail: warn

for p in /usr/libexec/rc/sbin/seedrng /usr/local/sbin/seedrng /usr/sbin/seedrng; do
	[ -x "$p" ] && exec "$p"
done

echo "seedrng: no seedrng binary found" >&2
exit 1
